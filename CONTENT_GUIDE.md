# 内容发布标准

本站的目标是沉淀可验证、可复用的技术经验，而不是扩大文章数量。

## Frontmatter

新文章至少包含：

```yaml
---
title: "清晰、具体、不夸大的标题"
description: "说明读者问题、文章范围和交付结果"
date: 2026-07-31
lastUpdated: 2026-07-31
verifiedAgainst: "产品或规范版本，验证日期"
---
```

- `date`：首次发布日。
- `lastUpdated`：最后一次实质内容更新日；纯格式调整不刷新。
- `verifiedAgainst`：教程、评测、协议和时效性内容必须填写。
- Starlight 会渲染 frontmatter 的 `title`，正文不要重复写一级标题。

## 写作风格

技术文章首先要让人愿意继续读，其次才是覆盖完整。推荐采用下面的叙事顺序：

```text
真实问题或写作缘起
  → 先说结论
  → 用问题组织章节
  → 代码、数据和案例
  → 作者的最终判断
```

### 开场

- 前两段先说明读者为什么会遇到这个问题，以及常见误区是什么。
- 可以使用第一人称表达判断，但不能编造没有发生过的项目经历、测试结果或失败案例。
- 不以百科定义、名词列表或“随着技术发展”开场。
- 在长文前增加“先说结论”，保留 3～5 条真正影响决策的判断，不复述目录。

### 正文

- 一个章节优先回答一个具体问题，例如“为什么会失败”“什么时候不该用”“怎么验证完成”。
- 先给场景和判断，再补术语；读者不应该为了理解第一段先查五个概念。
- 表格用于比较，Mermaid 用于解释关系，代码用于证明做法；没有信息增益时不要为了显得完整而添加。
- 少用连续清单。三个以上要点如果存在因果或先后关系，优先改成连贯说明或步骤。
- 明确写出取舍：选择了什么、放弃了什么、在哪些条件下结论不成立。

### 语气与收尾

- 语气可以自然、直接、有作者判断，但避免替读者制造焦虑。
- 不照搬固定标题模板；标题仍应包含问题、对象或结果。
- 结尾不要重复全文目录，应该留下一个可执行动作或清晰判断。
- “我认为”不能代替证据；事实、官方声明、实测和作者判断仍要明确区分。

## 内容类型

### 教程

必须包含：

- 明确版本和运行环境；
- 完整可执行命令；
- 预期结果；
- 失败路径或常见错误；
- 最后验证日期；
- 官方资料链接。

示例代码无法运行时，必须标注为伪代码。

### 评测与性能文章

必须包含：

- 被测产品和版本；
- 机器、系统、运行时和模型；
- 负载模型；
- 可复现脚本或仓库；
- 原始输出或数据文件；
- 多次运行和统计方式；
- 已知限制；
- 事实、测量与作者判断的明确区分。

没有数据时，不使用“实测”“压测”“提升 N 倍”等表达。

### 架构与经验文章

必须写清：

- 业务约束；
- 可选方案；
- 选择理由；
- 放弃了什么；
- 失败和恢复路径；
- 哪些结论来自真实系统，哪些是通用推导。

### 软件介绍

只总结 README 不足以构成独立文章。至少增加：

- 实际安装和使用；
- 一个完整场景；
- 局限与失败；
- 适用和不适用边界；
- 验证版本。

## 来源

- 技术规范、API 和产品行为优先引用官方文档、源码或论文。
- 时效性信息在发布前重新核对。
- 外部结论紧邻来源，不在文末堆一个无法对应正文的链接列表。
- 不引用无法定位的“业内数据”“实测结果”或“创始人采访”。

## 语言

- 使用简体中文，保留必要英文技术名词。
- 避免“终极、绝对安全、碾压、降维打击、必选、永远”等无条件表述。
- 标题优先描述问题和结果，不使用情绪化营销前缀。
- 区分“我验证了”“官方声明”“根据现有信息推断”。

## 代码与安全

- 不发布真实密钥、Cookie、内网地址和用户数据。
- 命令默认采用最小权限。
- 删除、生产写入、付款、发信等高风险操作必须说明审批和恢复方式。
- `curl | sh` 必须解释来源、风险和替代安装方式。
- SQL、Shell、MCP 和 Agent Tool 示例必须包含输入校验与权限边界。

## AI News

- 每日采集结果保存在 `src/data/ai-news-daily/`，不作为站点文章发布。
- `src/content/docs/ai-news/` 只保存周报和栏目入口。
- 周报必须去重，并增加工程影响和作者判断。
- 不手工修改自动生成周报；规则问题应修改 `scripts/fetch-ai-news.mjs`。

## 发布前检查

1. 文章只出现一个由 Starlight 渲染的页面标题。
2. `date`、`lastUpdated` 和必要的 `verifiedAgainst` 已填写。
3. 所有站内链接和图片可访问。
4. Mermaid、表格和代码块在桌面与移动端不溢出。
5. 命令和代码在声明的环境中验证。
6. 性能数字有原始证据。
7. 没有未解释的绝对化结论。
8. `npm run build` 通过。
9. `git diff --check` 通过。
10. 最终 diff 中只包含本次内容。
